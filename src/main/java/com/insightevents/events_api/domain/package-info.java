/**
 * Capa de dominio: entidades JPA que modelan el negocio
 * (Evento, Categoria, Analista, Asignacion, HistorialEvento) y sus enums.
 *
 * <p>Son el reflejo de las tablas en PostgreSQL y no deben exponerse
 * directamente en la API: para eso existe la capa {@code dto}.
 */
package com.insightevents.events_api.domain;
