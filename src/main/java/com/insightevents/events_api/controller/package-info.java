/**
 * Capa web: controladores REST ({@code @RestController}).
 *
 * <p>Exponen los endpoints HTTP, validan la entrada con Bean Validation,
 * delegan en la capa de servicio y traducen el resultado a respuestas
 * HTTP. No acceden a repositorios ni contienen logica de negocio.
 */
package com.insightevents.events_api.controller;
